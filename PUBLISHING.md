# 首页发布与回退

正式域名：https://sdondon.com

继续使用现有 GitHub Pages 的 main 分支根目录发布。`CNAME` 不变，首页现为 home-v3 的静态构建，源码保存在 `homepage-source/`。主页不依赖本机端口或 Cloudflare 服务。

旧首页完整保存在 `index-legacy.html`，旧项目页面及资源保持原路径。需要回退时，把 `index-legacy.html` 复制为 `index.html` 后提交推送即可；无需删除新主页素材。

发布前至少检查八张图片加载、头像默认居中、卡片先居中再打开详情、详情关闭，以及桌面和手机布局。
