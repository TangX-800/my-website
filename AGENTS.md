# 项目长期开发说明

## 项目定位

- 本项目是部署在 GitHub Pages 上的个人作品展示网站。
- 网站重点是清晰地展示个人介绍、精选作品、项目案例和联系方式。
- 视觉风格保持简洁、高级、深色，并让作品内容成为页面主角。
- 所有改动都应兼顾桌面端与手机端体验。

## 技术范围

- 当前阶段只使用原生 HTML、CSS 和少量原生 JavaScript。
- 不引入 React、Vue、Svelte、Bootstrap、Tailwind CSS 等框架或 UI 库。
- 不增加打包、编译或复杂的构建流程；网站应能直接通过浏览器打开，并可直接部署到 GitHub Pages。
- JavaScript 只用于确有必要的轻量交互。能用 HTML 或 CSS 完成的功能，不额外使用 JavaScript。
- 优先使用浏览器原生能力，谨慎增加第三方依赖、外部脚本和外部字体。

## 代码组织与可维护性

- 保持代码适合零基础用户阅读、理解和修改。
- 使用语义清楚的 HTML 标签，以及简单、直观、一致的 CSS 类名。
- 样式按页面区域组织，并用简短的中文注释标明导航、首屏、作品区、页脚、响应式规则等部分。
- 常用颜色、间距、圆角和动画时长优先通过 CSS 自定义属性统一管理。
- 避免过度抽象、复杂选择器、难以理解的缩写和炫技式代码。
- 新增 JavaScript 时，应保持函数短小、命名明确，并说明它控制的页面功能。
- 如果项目以后拆分文件，优先采用简单结构，例如 `css/`、`js/`、`images/`，不要为了少量内容提前复杂化。

## 视觉与交互原则

- 以深色背景、克制的对比色、清晰的文字层级和充足留白为基础。
- 作品图片、项目标题和项目说明应比装饰元素更醒目。
- 动效保持轻量、自然，不影响阅读，也不依赖动效传达关键信息。
- 保证正文和背景之间有足够对比度；链接、按钮和键盘焦点应容易识别。
- 图片应设置合适的替代文本，并尽量控制文件大小，避免拖慢 GitHub Pages 的加载速度。
- 响应式布局至少检查常见手机宽度和桌面宽度，避免横向滚动、文字溢出和点击区域过小。

## 修改流程

每次修改都遵循以下流程：

1. 先阅读相关文件，确认当前结构和已有写法。
2. 修改前先向用户说明计划，包括准备修改哪些文件、解决什么问题，以及是否会影响现有内容。
3. 只修改完成任务所需的内容，不顺手改动无关部分。
4. 修改后检查页面结构、样式、链接和基本响应式表现。
5. 向用户说明修改结果，并提供清楚的预览方法。

## 预览与检查

- 简单预览：直接用浏览器打开项目根目录中的 `index.html`。
- 更可靠的本地预览：在项目目录启动一个静态文件服务器，然后在浏览器访问它提供的本地地址。
- 发布预览：推送到 GitHub 后，通过仓库的 GitHub Pages 地址检查线上效果。
- 每次预览至少检查：首页首屏、导航链接、作品卡片、联系方式、手机布局和桌面布局。
- 如果新增其他页面，还要检查页面之间的相对链接在本地和 GitHub Pages 上是否都能正常工作。

## 当前项目注意事项

- 当前项目主要内容位于根目录的 `index.html`，CSS 也内嵌在该文件中。
- 当前源码中可见中文乱码和疑似不完整的 HTML 标签。处理这些内容前，应先确认文件编码和原始文案，避免直接覆盖用户想保留的文字。
- 当前页面的浅色样式与长期目标中的深色方向不完全一致；未来调整时应分步骤进行，并在修改前明确说明范围。
- 未经用户明确要求，不删除现有内容、不替换作品信息，也不改变网站部署方式。

## 完成标准

一次修改只有在满足以下条件时才算完成：

- 符合纯 HTML、CSS、少量 JavaScript 的技术范围。
- 不依赖框架或构建工具。
- 页面在手机端和桌面端均可正常阅读和操作。
- 代码结构清楚，零基础用户可以找到并修改主要文案、颜色和图片。
- 已向用户说明修改内容和预览方式。
